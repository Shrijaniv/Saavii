# apps/api/src

Service source. `routes/` holds one module per Volume IV API domain. Server bootstrap, `trust` auth middleware wiring, and error handling live at this level. See `apps/api/README.md` for the contract.
