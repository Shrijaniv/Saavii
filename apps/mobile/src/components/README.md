# components

Shared UI: action cards (chat responses), block controls (complete/partial/skip/reschedule/explain), explanation and confidence surfaces. Components render explanations wherever the API provides them — an unexplained recommendation is a bug (Volume I, Ch. 4, Principle 7).
