# screens/calendar

A combined timeline of fixed reality and generated work (Volume III): classes, external meetings, meals, commute, personal commitments, Saavii-generated work blocks, buffers.

Rules: only actionable work blocks receive completion controls; fixed commitments remain visually distinct. This screen renders the canonical plan from the API — it never reads Google Calendar directly.
