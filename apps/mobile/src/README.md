# apps/mobile/src

Client source. `navigation/` (5-tab IA), `screens/` (one folder per Volume III screen spec), `components/` (shared UI), `notifications/` (client-side push). See `apps/mobile/README.md` for the contract.
