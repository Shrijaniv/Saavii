/**
 * Module: conflicts/conflict-engine
 * Responsibility: Pure detection of deadline mismatches, overrides, overlaps, source changes, intent conflicts. Important conflicts never silently resolved (Vol IV).
 *
 * PLACEHOLDER — no implementation yet. Read this folder's README.md and
 * /ARCHITECTURE.md before implementing. /CLAUDE.md constraints apply.
 */
